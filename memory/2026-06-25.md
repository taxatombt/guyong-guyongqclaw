## R47 审查质量复盘（22:29 用户纠正）

### 用户评分：7/10

### 正确部分
- P1: 两个 regime 来源（regime_detector vs compute_signal 内置）→ 设计隐患 ✅
- P2: SYMBOL 全局变量被双币选优改写 → 耦合问题 ✅
- 代码安全可跑，系统完整 ✅

### 错误部分
- **核心论点不成立**：说"state.json 里存了 2.0 覆盖 champion"是错的
  - state.json 根本没有 `_long_thresh` 字段，是我脑补的
  - 阈值每轮由 `get_regime_threshold(current_regime)` 动态查表生成
  - champion 的 SIGNAL_THRESHOLD=1.5 只是查表的 fallback（regime 不在表中时兜底）
  - 信号 0.6 过不了的真实原因：bull→1.0 门槛已经是最低的，0.6 连 1.0 都过不了，信号本身太弱，不是阈值被锁在高位

## R48 完整审查（22:40-22:47，全文件通读）

### 审查概况
- **文件**：E:\lianghua\trend_trader.py（5215行）
- **方法**：R34方法论（Simple Stupid First + 逐段eyeball + 数值验证），分7段通读完全文件
- **综合评分**：8.5/10 ✅ **实盘就绪**

### 覆盖范围（7段）
1. L1-L700: 导入模块、get_klines、技术指标（ema/adx/rsi/macd/volume/bb/微观结构）
2. L700-L1500: POTENTIAL_EXIT_MAP、_compute_exit_prices、compute_trend_potential、compute_signal
3. L1500-L2400: compute_signal_with_ml、_build_ml_features、multi_tf_signal_matrix、wait_for_fill、attach_stop_loss_profit
4. L2400-L2830: _handle_system_failure、close_position、check_stop_loss_and_profit、check_global_stop_loss、check_max_hold_timeout
5. L2830-L3630: trading_loop前半段（预检→K线→Regime检测→信号计算→双币并行→阈值选择）
6. L3630-L4190: trading_loop后半段（MAO预检→Iron Laws→多头/空头开仓→仓位计算→止盈止损挂单）
7. L4190-L5215: GUI 12Tab + compute_trend_potential + on_exit + 看门狗 + __main__

### 核心确认（全部正确 ✅）
1. ✅ 信号引擎对称评分（7维方向对称加减分，多头空头无偏向）
2. ✅ POTENTIAL_EXIT_MAP完整定义（4个级别：噪声/短期/中期/长期，sl_atr/tp_atr/trail数值正确）
3. ✅ 多空止损止盈方向正确（数值验证：多头止损在下方SL=entry-sl_atr×atr，空头止损在上方SL=entry+sl_atr×atr）
4. ✅ 12层风控链（预检/熔断/MAO/Iron Laws/Circuit Breaker/置信度/OBI/宏观日历/情绪过滤/周末强制/全局止损/持仓超时）
5. ✅ 双币种BTC+ETH并行架构（独立专用state字段、双币选优、ETH专属阈值加载、双币全扫残留仓检测）
6. ✅ 崩溃恢复逻辑（check_pending_order_on_startup + _recover_state_on_connect + _recover_open_orders_on_connect + 看门狗）
7. ✅ 系统故障三层兜底：市价平仓→条件止损→通知告警
8. ✅ Kelly仓位 × LEVERAGE 修复已生效
9. ✅ 看门狗随GUI启动/退出（subprocess + on_exit terminate）

### 无新增P1 ✅
- `compute_signal_with_ml()` → `_build_ml_features()` 内有完整try/except保护，不崩溃
- PTP分支 `return False` 已删除，`deserialize_tiers`已导入

### P2（2项）
1. **SYMBOL全局变量被双币选优改写**：直接给全局变量赋值以切换当前币种。当前工作正常，但全局变量可写性在复杂多线程架构中是耦合风险
2. **regime_detector vs compute_signal内置regime两套**：`get_regime_and_params()` 从外部检测regime用于阈值查表，`compute_signal`内部有独立的 `detect_regime` 用于信号评分。阈值表配的是一根线，信号评分用的是另一根线

### 方法论验证 ✅
坚持R34正确方法论：Simple Stupid First + 逐段eyeball + 数值验证，分7段通读完全文件，未写复杂脚本，验证了该方法论在5000+行大型代码审查中的持续有效性

### 审查方法论教训（追加）
1. 查 state.json 字段前，先 grep 确认字段是否真的被序列化写入，不凭调用链脑补
2. 阈值路径追全：不要停在 `state.get("_long_thresh", SIGNAL_THRESHOLD)` → 走到上游 `get_regime_threshold → _LONG_THRESHOLDS → current_regime`
3. "信号过不了门槛"这种表述要谨慎 → 先确认门槛范围（bull=1.0 已经很低），再判断信号太低还是门槛太高
## R48 全文件审查（23:25 完成）

- **时间**：2026-06-25 23:25 GMT+8 完成
- **审查对象**：trend_trader.py（5215 行），分7段通读全文件
- **审查方法**：R34 方法论（Simple Stupid First + 逐段 eyeball + 数值验证）
- **综合评分**：8.5/10 ✅ **实盘就绪**
- **核心确认**：
  - 信号引擎7维度对称评分（多头/空头加减分一致）
  - 止损止盈方向：多头 SL=entry-atr（下方前）、空头 SL=entry+atr（上方前），数值验证通过
  - POTENTIAL_EXIT_MAP L1509 完整定义（0-噪声/1-短期/2-中期/3-长期，含 sl_atr / tp_atr）
  - 12层风控链完整（预检→熔断→置信度→HTF→Circuit Breaker→MAO→Iron Laws→OBI→情绪→宏观→Agent→Kelly）
  - 双币种并行架构：独立 state 专用字段（position_{suf}/qty_{suf}/entry_{suf}/SL_{suf}）、符号后缀 helper函数、双币全扫残留仓告警、ETH专属阈值
  - 崩溃恢复三层兜底：市价平仓（3次）→条件止损→通知
  - 看门狗随GUI启动（`__main__` 中 `subprocess.Popen`），随 `on_exit()` 一并 `terminate()`
  - Kelly × LEVERAGE 修复生效（`kelly_notional = min(kelly_val * LEVERAGE * LEVERAGE_SAFETY, ...)`）
  - fail-safe 连续预检失败5次且持仓 → 紧急平仓
- **无新增P1**：
  - `compute_signal_with_ml`（L2361+）有 try/except 保护
  - `compute_ema` 在 `build_ml_features` 中有自己的 try/except
  - PTP分支 `return False` 已修复
  - `deserialize_tiers` 已从 `progressive_take_profit` 导入
- **2项P2**：
  1. SYMBOL 全局变量被双币选优直接改写
  2. regime_detector 和 compute_signal 内部有两套独立 regime 检测
- **报告**：已追加到 memory/2026-06-25.md

---

## 失败清单记录

### 2026-06-25 R47 审查质量不足（22:29 复盘）
- **错误**：误判 `_long_thresh` 字段覆盖 champion 参数
- **根因**：脑补 state.json 有 `_long_thresh` 字段，实际未序列化写入；阈值路径没追全到 `get_regime_threshold`
- **教训**：查 state.json 字段前先 grep 确认是否被序列化；阈值路径追全到查表源头；表述"信号过不了门槛"前先确认门槛范围

### 02:04  -  编程套餐推荐

- 用户需要便宜好用的coding plan推荐，类似火山引擎coding plan
