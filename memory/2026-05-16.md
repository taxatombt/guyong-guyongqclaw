# 2026-05-16 日记

## 零点自动清理与回顾

**执行时间：** 2026-05-16 00:00

### 检查结果

| 检查项 | 状态 | 说明 |
|--------|------|------|
| _deprecated/ 目录 | ❌ 不存在 | 无需清理 |
| workspace 临时文件 | ✅ 无 | 上次 _tmp_check_sizes.py 已删 |
| _download/ 目录 | ✅ 空 | 无需清理 |
| heartbeat-state.json | ✅ 已更新 | lastCleanup → 2026-05-16 |

### ⚠️ 磁盘状态（稳定中断！）

| 盘 | 空闲 | 占比 | vs 昨天 |
|----|------|------|---------|
| C: | 12.7GB | 8.4% | **-1.7GB** ⚠️ |
| D: | 44.6GB | 2.4% | 持平 |
| E: | 39.1GB | 12% | **-3.4GB** ⚠️ |
| F: | 412GB | 44.2% | **-117.4GB** 🔴 |

**连续稳定仅维持了2天即中断。F盘一天减少117GB，变化剧烈。**
可能原因：大文件下载/缓存写入（需白天排查）。

### ⚠️ self_review 过期警告

lastSelfReview = 2026-05-10，**已过期6天**。下次心跳时提醒。

---

## 2026-05-15 回顾（昨日总结）

**昨日是高产出的一天，主要围绕 QwenPaw 故障排查和 mem0 深度研究。**

### 核心成果

1. **QwenPaw 启动排查（20:31-22:45，超2小时）**
   - 发现4个根因：VBS隐藏窗口、配置目录 .copaw vs .qwenpaw 不匹配、端口动态分配、首次缺少 init
   - 解决：python -u -m qwenpaw init --defaults → desktop 启动 → http://127.0.0.1:9185 ✅
   - 关键认知：QwenPaw ≅ CoPaw，共享配置目录、80技能、MiniMax-M2.7 LLM

2. **mem0 源码级深度研究（19:12-19:28 + 22:43-22:54）**
   - 读了4个核心源文件：main.py / scoring.py / entity_extraction.py / prompts.py
   - 提出4项落地方案：多信号融合检索、实体链接、Agent事实一等公民、Add-only提示词
   - 🔥🔥🔥 对 qclaw ③记忆层有重大贡献

3. **Python 路径最终确定（02:11）**
   - 小谷确认：E:\PYTON\python.exe（Python 3.13.2）
   - E:\qwenpaw\python.exe = QwenPaw 内置（3.10.20）

4. **项目分级处理规则确立（04:25）**
   - 小谷：如果对系统没什么特别的进步进化作用，不用做了，光学备用
   - 新标准：🔥🔥🔥 级贡献才正式纳入

### 今日教训

1. **VBS 隐藏窗口** — 调试时不用快捷方式，直接 cmd/PS
2. **配置路径 ≠ 检查路径** — bat 查 .qwenpaw，实际存 .copaw
3. **端口不能硬编码** — QwenPaw 动态端口，要读启动日志
4. **源码 > README** — mem0 的 scoring.py 多信号融合公式才是精髓

---

## 待办追踪（截至 5/15）

| # | 事项 | 优先级 | 状态 |
|---|------|--------|------|
| 1 | mem0 落地方案（4项）| 高 | ⏳ 待小谷确认 |
| 2 | Blender 下载安装 | 中 | pending |
| 3 | mcporter + blender-mcp 测试 | 中 | pending |
| 4 | qclaw 集成 blender-mcp | 中 | pending |
| 5 | 技能模块9合3整合 | 低 | pending |
| 6 | event_bus + session_vault 合并 | 低 | pending |
| 7 | ruflo: guidance-enforcer 重跑 | 低 | pending |
| 8 | ruflo: hnsw-vector 重跑 | 低 | pending |
| 9 | SKILL-gbrain.md 创建 | 低 | pending |
| 10 | SYSTEM.md ②-⑥层表格更新 | 低 | pending |

---

## 今日计划

- 等待小谷指示
- 注意磁盘变化趋势（F盘异常）
- 如心跳触发，提醒 self_review 已过期6天
- 有新任务按 🔥🔥🔥 标准判断是否纳入系统

---

_日记创建于 2026-05-16 00:00_
## 00:30-00:53 mem0 4项落地全部完成 ✅

小谷确认"该做就做"后，顾庸在凌晨完成了 mem0 全部代码落地。

### 落地成果

| # | 落地项 | 文件 | 说明 |
|---|--------|------|------|
| 1 | 多信号融合检索 | `mem0_hybrid_search.py` | BM25 + 语义 + 实体 boost 三路融合 |
| 2 | 实体提取与链接 | `mem0_entity_extractor.py` | 规则层(4类实体) + LLM层 + EntityStore |
| 3 | Agent 事实一等公民 | `evolver.py` 修改 | `record_agent_fact()` 模块函数 + `EvolverEngine.record()` 自动调用 |
| 4 | Skill 文档 | `skills/SKILL-mem0.md` | 6KB 完整文档 |
| 5 | 贡献矩阵 | `ai_agent_study/SYSTEM.md` | ③记忆层(🔥🔥🔥) + ⑥进化层(🔥) |

### 新增/修改文件清单

**新增 3 个：**
- `mem0_hybrid_search.py` — HybridSearcher 类，sigmoid 归一化 BM25，entity boost 0.5
- `mem0_entity_extractor.py` — extract_entities_rule() + extract_entities_llm() + EntityStore
- `skills/SKILL-mem0.md` — 完整使用文档（安装/配置/接口/故障排查）

**修改 2 个：**
- `evolver.py` — 新增 `record_agent_fact()` / `agent_facts_stats()` / `FACTS_PATH`，4处 edit
- `ai_agent_study/SYSTEM.md` — ③记忆层表格 + 贡献矩阵 + 落地状态表

**依赖安装：**
- `pip install rank-bm25` ✅

### 测试结果

- `mem0_hybrid_search.py "小谷"` → 155 文档段，BM25 检索正常返回
- `mem0_entity_extractor.py` → 4类实体提取正常（PROPER/QUOTED/COMPOUND/NOUN）
- `evolver.py facts` CLI → Agent Facts: total=2, success=1, failure=1 ✅

### 关键设计决策

1. **不创建 `mem0_agent_facts.py`** — 逻辑合入 `evolver.py` 模块函数，减少文件碎片
2. **`EvolverEngine.record()` 自动调用** — 每次记录规则时自动提取 Agent 事实
3. **memory_pipeline.py 不做** — qclaw 存储已是 add-only（追加文件），无需额外修改
4. **BM25 用 rank-bm25 而非手动实现** — 与 mem0 原版一致

### 核心技术点

- **融合公式**（来自 mem0 scoring.py）：`combined = (semantic + normalized_bm25 + entity_boost) / max_possible`
- **BM25 归一化**：sigmoid 函数，query-length 自适应参数（short/medium/long 三档）
- **阈值门控**：语义分 < 0.5 直接排除，BM25/实体不能"救活"
- **实体 boost**：每个匹配实体 +0.5 权重

---

## 待办更新

| # | 事项 | 优先级 | 状态 |
|---|------|--------|------|
| 1 | **mem0 落地方案（4项）** | 高 | ✅ 已完成（2026-05-16 00:53） |
## 01:10-01:24 lianghua 项目源码审查

小谷要求审查 lianghua 量化项目的源码并提出建议。按规矩（顾庸x的项目，只给建议不动手），完成了全面代码审查。

### 文件读取

- `E:\lianghua\config.py`（~90行）：参数配置中心
- `E:\lianghua\indicators.py`（~280行）：6 regime状态机 + 动态ATR/仓位 + 加权打分
- `E:\lianghua\STRATEGY.md`：策略文档（含7个参考项目总结）
- `E:\lianghua\trend_trader.py`（~700行）：主程 v2.2（GUI+凯利+多周期RSI+MACD背离）
- `E:\lianghua\trend_trader_v2.py`（~500行）：旧版 v2.0（简化版）

### 发现的P0问题（6个）

1. **K线周期矛盾**：STRATEGY.md/config.py 写 4h，但 trend_trader.py 用 `interval="1h"` — 策略核心参数不一致
2. **config.py 无人引用**：trend_trader.py 完全重定义了所有参数，两处独立维护
3. **跟踪止盈 bug**：trailing_price 只在开仓初始化一次，从不更新，导致跟踪止盈实际无效
4. **固定止盈缺失**：check_stop_loss_and_profit() 只检查了止损+跟踪止盈，没检查 TAKE_PROFIT_ATR
5. **API Key 明文存储**：api_key.txt + api_secret.txt 明文在项目目录
6. **testnet 硬编码**：create_binance_client() 中 testnet=True 硬编码，与 PAPER_SIMULATE 无关

### P1问题（4个）

7. STRATEGY.md 与代码 5 处不一致（杠杆3x→1.5x，每日2笔→5笔，K线4h→1h，只做多→多空双向，连亏2→3笔）
8. 指标函数在 indicators.py 和 trend_trader.py 中重复实现且 RSI 处理方式不同
9. state.json 恢复无校验（过期熔断日期不会被清理）
10. 计数器逻辑分散在 close_position 调用方而非集中管理

### P2优化（5个）

- 回测缺失、异常处理过宽、GUI耦合、仓位太轻(0.005 BTC)、MACD算法不一致

### 审查结论

优先修复顺序：K线周期统一 → import config.py → 加固定止盈 → 修复跟踪止盈 → testnet 动态切换 → 更新 STRATEGY.md → 统一信号计算 → state.json 校验
## 01:30-01:44 lianghua 补充审查

读完了 IMPLEMENTATION_PLAN.md、mimic_trader.py、state.json、trades.csv 后，出具第二版建议。

### 新发现

1. **IMPLEMENTATION_PLAN.md 计划与代码脱节**：计划文档反复引用 `analyze_market()`/`open_position()`/`check_and_close()`，但 trend_trader.py 实际是 trading_loop() 内联逻辑，没拆出这些函数。计划写"已写入"但代码结构对不上。

2. **mimic_trader.py 不是跟单机器人**：只是 auto_start 功能的测试脚本，项目名称有误导。

3. **trades.csv 只有 SELL 记录**：3条全是 SELL(paper)，没有 BUY。开仓日志可能漏记。

4. **跟踪止盈 bug 已确认（实盘直接亏钱）**：trailing_price 只在开仓设一次从不更新，价格创新高后永远不会触发新的跟踪条件。计划文档讨论的是倍数自适应，没触及这个根本逻辑缺陷。

5. **state.json 极简**：只有4个字段（auto_start/connected/running/position），20+字段的关键状态（持仓价/止损价/跟踪价）崩溃后全部丢失。

### 最终8项修复顺序

① K线周期统一 ② from config import * ③ 固定止盈补检查 ④ 跟踪止盈逻辑修复 ⑤ 统一信号计算(用indicators.py) ⑥ testnet动态切换 ⑦ 代码结构对齐IMPLEMENTATION_PLAN ⑧ state.json完整保存
## 01:50-01:55 lianghua 第三轮审查 — 架构层面发现

读完 config.py、indicators.py、monitor.py 后，发现了比单点 bug 更严重的结构性问题：

### 核心问题：两套独立信号系统并存

indicators.py（280行，numpy ewm标准实现，6状态机，自适应ATR/仓位，分函数规范）完全没被 trend_trader.py import。趋势代码把所有指标、参数、逻辑全部重写了一遍。

**indicators.py 有但 trend_trader.py 没用上的：**
- 6 regime 状态机 vs 简单的 TREND/RANGE 二分
- get_position_size_pct() 动态八档仓位 vs 凯利公式
- should_forbid_new_position() 统一入口 vs 零散检查
- get_adaptive_atr_multipliers() vs 全局固定值
- is_bear_market() 独立函数 vs 内联逻辑

**关键证据：** monitor.py 成功 import indicators 并用 4h K线，而 trend_trader.py 用的是 1h。同一项目两个周期。

**indicators.py 实际已实现了 IMPLEMENTATION_PLAN 90% 的改进！只需 import 就行。**

### 本轮新发现的遗漏问题
- MIN_TRADE_INTERVAL=7200 在 config 但无人 enforce
- GLOBAL_STOP_LOSS=0.08 无任何检查
- API_MAX_RETRIES=3 等重试机制未落地
- 布林带阈值三处三种表达方式

### 最终修复路线
① trend_trader.py import config + import indicators，删所有重复
② 补 GLOBAL_STOP_LOSS / MIN_TRADE_INTERVAL / tracking / TP / testnet
## 02:00-02:20 lianghua 第四轮审查 — 文档与版本演进

读完 VERSION_LOG v2.0-v2.3 和 STRATEGY.md，发现新层面问题：

### 版本迭代时间线（全部今天）
v2.0(22:20) → v2.1(22:35) → v2.2(23:50) → v2.3(00:40)
四版在 2h20m 内完成，持续加功能，从未重构架构。

### 核心发现

**1. STRATEGY.md 几乎全错**
- 杠杆: 文档3x vs 代码1.5x
- 方向: 文档"只做多" vs 代码多空双向
- 每日笔数: 文档2笔 vs 代码3-5动态
- K线: 文档4h vs 代码1h
- 净值跌8%平仓: 文档铁律 vs 代码无检查
- 文件结构: 文档列5个 vs 实际80+

**2. 文件组织失控** — 80+文件，大量测试/临时脚本（click_*.py, watch*.ps1, *_err.log等）。推荐核心文件≤15个，其余移入_archive/。

**3. 跟踪止盈bug从v2.0就有** — 原始代码已确认trailing_price只设一次不更新，历经4版未修复。

**4. GLOBAL_STOP_LOSS=0.08** — 文档和config都有，但4个版本代码中均无实现。

**5. stock-daily-analysis-skill** — 独立的GitHub项目被整个嵌入lianghua根目录。

### 五层修复路线
① 文档对齐(STRATEGY.md) ② 文件清理 ③ 代码缝合(import config+indicators) ④ 补漏(tracking/TP/GLOBAL_STOP/testnet) ⑤ 安全(API Key环境变量)
## 02:25-02:45 lianghua 第五轮审查 — 重大纠正

### 关键纠正：之前审查基于 v2.0 备份，v2.3 实际已修复大量问题

读取 trend_trader.py v2.3 实际代码（55KB/1305行）后发现：

**已修复（我之前批错了）：**
- `from config import *` ✅ (L22)
- `from indicators import calculate_signal, compute_all_indicators` ✅ (L15)
- `from indicators import is_bear_market, detect_regime` ✅ (L1078)
- indicators.py 六态门控已接入：`should_forbid_new_position()` ✅ (L1087)
- `testnet=PAPER_SIMULATE` 动态切换 ✅ (L229)
- 动态仓位 `compute_position_from_balance()` ✅ (L245)
- MIN_TRADE_INTERVAL 已实现 ✅ (L1119-1128)
- **跟踪止盈已修复** ✅ (L948-949: `state["trailing_price"] = price`)
- 凯利/多周期RSI/自适应ATR 全部存在且被调用 ✅
- 科学熔断 _record_atr_stop 已实现 ✅

**仍存在的问题（仅4个）：**
1. 固定止盈TP：`take_profit_price` 存储了但 `check_stop_loss_and_profit()` 不检查
2. GLOBAL_STOP_LOSS：函数存在(L972)但空壳，peak_balance不更新不比较
3. K线周期：代码仍用1h，config和文档写4h
4. STRATEGY.md：严重过时（"只做多"/"3x"/"每天2笔"全错）

### 教训
审查前需确认读的是实际运行版本，不是备份。v2.3 在架构缝合上做得比预期好很多。
## 02:45-03:00 lianghua 第六轮审查 — IMPLEMENTATION_PLAN 落地验证

读完 IMPLEMENTATION_PLAN.md（完整版）和 state.json 后，做了 PLAN 声称 vs 实际代码的对照。

### 核心发现：PLAN 里 2/4 项"已写入"并不存在

| 改进 | PLAN说法 | 实际v2.3代码 |
|------|---------|------------|
| ① 六态市场识别 | ✅ 已写入 | ✅ should_forbid_new_position() (indicators.py) |
| ② 动态ATR倍数 | ✅ 已写入 | ❌ get_adaptive_atr_multipliers 不存在 |
| ③ 出场实时判断 | ✅ 已写入 | ✅ prev_trend 已移除 |
| ④ 双指标确认 | ✅ 已写入 | ❌ ema_confirm & macd_confirm 不存在 |

②和④在PLAN文档中列举了完整代码示例但未写入v2.3。

**PLAN 函数命名与代码对不上**：
- PLAN: analyze_market() / open_position() / check_and_close()
- 实际: trading_loop() 内联全部逻辑，未拆分函数
- PLAN文档对后来者具有误导性

### 新发现

**1. state.json 持久化不完整**
- 开仓时 `state["take_profit_price"]` 存入(L1107)但 state.json 无此字段
- ema20/50/200, account balance, peak_balance 均未持久化
- 程序崩溃后持仓恢复会丢失止盈目标价
- 当前 state.json 有12个字段但缺关键交易状态

**2. 日志噪音严重**
- 每轮输出4条日志（7分制+100分制+多RSI+状态枚举）
- 15秒循环 = 每天23,040行日志
- 建议加 verbose 开关

**3. ATR使用不一致**
- 开仓时用标准ATR（compute_signal返回值）
- 持仓检查时用自适应ATR（compute_adaptive_atr）
- 同一仓位开仓止损和后续止损ATR计算方式不同

### 六轮审查覆盖矩阵

| 轮次 | 视角 | 关键发现 |
|------|------|---------|
| 1 | 代码bug | 跟踪止盈、TP缺失、testnet硬编码等6个P0 |
| 2 | 架构 | config/indicators未对接，IMPLEMENTATION_PLAN脱节 |
| 3 | 深层架构 | 两套信号系统并存，indicators.py已实现90%改进 |
| 4 | 文档/版本 | STRATEGY.md严重过时(6处错误)，80+文件混乱 |
| 5 | 自我纠正 | v2.3已修80%问题，之前看的是v2.0备份 |
| 6 | 落地验证 | PLAN2/4项虚报，state持久化不完整，日志噪音 |

### 当前实际待修（准确版）

v2.3 真正剩下：
1. 固定止盈TP检查（take_profit_price存了但不用）
2. GLOBAL_STOP_LOSS空壳（peak_balance从不更新比较）
3. K线周期统一为4h
4. STRATEGY.md同步更新
5. state.json补充take_profit_price等关键字段
6. IMPLEMENTATION_PLAN ②④两项落地或从PLAN中移除
## 10:37-10:55 lianghua 第七轮审查 — 代码质量 + 安全审计

### 🔴 严重问题

**1. state.json 非原子写入 — 崩溃数据全丢**
- `save_state()` (L197): 直接 `open(STATE_FILE, "w")` + `json.dump`
- 没有 `.tmp` + `os.replace()` 原子模式
- 没有备份上次成功保存的状态
- 写到一半崩了 = state.json 损坏 = 重启后恢复不了持仓

**2. 线程安全：state 字典零保护**
- 88 次访问 state 字典
- `_client_lock` 只保护 Client 对象（L223，只用了1次）
- trading_loop 线程不断写 state，GUI 线程同时读
- 没有任何锁保护 state，读脏数据概率低但后果严重

**3. API Key 在 GUI 明文显示**
- `api_key_entry` (L1206): **没有 `show="*"`** — 输入的 Key 明文可见
- `api_secret_entry` (L1213): 有 `show="*"` — Secret 遮住了
- Key 和 Secret 同样敏感，应都遮住

### 🟠 高优先级

**4. 裸 `except Exception` — 22 处**
- 8处连日志都不打（L192,206,287,300,325,912,1015,1127）
- `save_state()` 的 `except Exception: pass` — 状态保存失败完全不知道

**5. 30 秒 debounce — 可以丢关键状态**
- L200: `if now - _last_save_time < 30: return`
- 一个订单的完整生命周期可能 < 30 秒
- 如果在这 30 秒内崩溃，订单状态全丢

**6. requirements.txt 依赖缺失**
- 只有：`python-binance>=1.0.19`
- 代码实际需要：`pystray` ❌ `Pillow (PIL)` ❌ `numpy` ❌ `requests` ❌
- 其他人拿到 requirements.txt 装不全依赖

### 🟡 中优先级

**7. 内联 import — 散乱且可能重复**
- `from indicators import ...` 在 L15 和 L1083 两次 import
- `import requests` 在 L279/292/317 三次 import
- 所有 import 应该在文件顶部

**8. 没有信号处理器**
- Ctrl+C / 进程 kill → `save_state()` 不调用
- `on_exit()` 依赖 GUI 的 `root.destroy()`

**9. get_klines() 无超时控制**
- 如果 Binance API 响应慢，get_klines() 会阻塞整个循环
- 持仓期间阻塞 = 止损止盈检查暂停

### 七轮审查总览

| 轮次 | 视角 | 发现重点 |
|------|------|---------|
| 1 | 代码bug | 跟踪止盈、TP缺失、testnet硬编码 |
| 2 | 架构 | config/indicators未对接 |
| 3 | 深层架构 | 两套信号系统并存 |
| 4 | 文档/版本 | STRATEGY.md过时，80+文件 |
| 5 | 自我纠正 | v2.3已修80%，之前看的是v2.0备份 |
| 6 | 落地验证 | PLAN里2/4项虚报 |
| 7 | **质量/安全** | **线程安全+非原子写+Key明文+22处裸except** |

### 最终修复优先级（准确版）

1. state.json 原子写入 + 备份
2. state 字典加锁（或用 `threading.Lock()`）
3. API Key 输入框加 `show="*"`
4. 裸 `except` 补日志
5. debounce 从30秒降到5秒或去掉
6. requirements.txt 补全依赖
7. import 统一到文件顶部
8. 加信号处理器（SIGINT/SIGTERM）
9. get_klines() 加超时
﻿# 2026-05-16 日记

## 零点自动清理与回顾

**执行时间：** 2026-05-16 00:00

### 检查结果

| 检查项 | 状态 | 说明 |
|--------|------|------|
| _deprecated/ 目录 | ❌ 不存在 | 无需清理 |
| workspace 临时文件 | ✅ 无 | 上次 _tmp_check_sizes.py 已删 |
| _download/ 目录 | ✅ 空 | 无需清理 |
| heartbeat-state.json | ✅ 已更新 | lastCleanup → 2026-05-16 |

### ⚠️ 磁盘状态（稳定中断！）

| 盘 | 空闲 | 占比 | vs 昨天 |
|----|------|------|---------|
| C: | 12.7GB | 8.4% | **-1.7GB** ⚠️ |
| D: | 44.6GB | 2.4% | 持平 |
| E: | 39.1GB | 12% | **-3.4GB** ⚠️ |
| F: | 412GB | 44.2% | **-117.4GB** 🔴 |

**连续稳定仅维持了2天即中断。F盘一天减少117GB，变化剧烈。**
可能原因：大文件下载/缓存写入（需白天排查）。

### ⚠️ self_review 过期警告

lastSelfReview = 2026-05-10，**已过期6天**。下次心跳时提醒。

---

## 2026-05-15 回顾（昨日总结）

**昨日是高产出的一天，主要围绕 QwenPaw 故障排查和 mem0 深度研究。**

### 核心成果

1. **QwenPaw 启动排查（20:31-22:45，超2小时）**
   - 发现4个根因：VBS隐藏窗口、配置目录 .copaw vs .qwenpaw 不匹配、端口动态分配、首次缺少 init
   - 解决：python -u -m qwenpaw init --defaults → desktop 启动 → http://127.0.0.1:9185 ✅
   - 关键认知：QwenPaw ≅ CoPaw，共享配置目录、80技能、MiniMax-M2.7 LLM

2. **mem0 源码级深度研究（19:12-19:28 + 22:43-22:54）**
   - 读了4个核心源文件：main.py / scoring.py / entity_extraction.py / prompts.py
   - 提出4项落地方案：多信号融合检索、实体链接、Agent事实一等公民、Add-only提示词
   - 🔥🔥🔥 对 qclaw ③记忆层有重大贡献

3. **Python 路径最终确定（02:11）**
   - 小谷确认：E:\PYTON\python.exe（Python 3.13.2）
   - E:\qwenpaw\python.exe = QwenPaw 内置（3.10.20）

4. **项目分级处理规则确立（04:25）**
   - 小谷：如果对系统没什么特别的进步进化作用，不用做了，光学备用
   - 新标准：🔥🔥🔥 级贡献才正式纳入

### 今日教训

1. **VBS 隐藏窗口** — 调试时不用快捷方式，直接 cmd/PS
2. **配置路径 ≠ 检查路径** — bat 查 .qwenpaw，实际存 .copaw
3. **端口不能硬编码** — QwenPaw 动态端口，要读启动日志
4. **源码 > README** — mem0 的 scoring.py 多信号融合公式才是精髓

---

## 待办追踪（截至 5/15）

| # | 事项 | 优先级 | 状态 |
|---|------|--------|------|
| 1 | mem0 落地方案（4项）| 高 | ⏳ 待小谷确认 |
| 2 | Blender 下载安装 | 中 | pending |
| 3 | mcporter + blender-mcp 测试 | 中 | pending |
| 4 | qclaw 集成 blender-mcp | 中 | pending |
| 5 | 技能模块9合3整合 | 低 | pending |
| 6 | event_bus + session_vault 合并 | 低 | pending |
| 7 | ruflo: guidance-enforcer 重跑 | 低 | pending |
| 8 | ruflo: hnsw-vector 重跑 | 低 | pending |
| 9 | SKILL-gbrain.md 创建 | 低 | pending |
| 10 | SYSTEM.md ②-⑥层表格更新 | 低 | pending |

---

## 今日计划

- 等待小谷指示
- 注意磁盘变化趋势（F盘异常）
- 如心跳触发，提醒 self_review 已过期6天
- 有新任务按 🔥🔥🔥 标准判断是否纳入系统

---

_日记创建于 2026-05-16 00:00_
## 00:30-00:53 mem0 4项落地全部完成 ✅

小谷确认"该做就做"后，顾庸在凌晨完成了 mem0 全部代码落地。

### 落地成果

| # | 落地项 | 文件 | 说明 |
|---|--------|------|------|
| 1 | 多信号融合检索 | `mem0_hybrid_search.py` | BM25 + 语义 + 实体 boost 三路融合 |
| 2 | 实体提取与链接 | `mem0_entity_extractor.py` | 规则层(4类实体) + LLM层 + EntityStore |
| 3 | Agent 事实一等公民 | `evolver.py` 修改 | `record_agent_fact()` 模块函数 + `EvolverEngine.record()` 自动调用 |
| 4 | Skill 文档 | `skills/SKILL-mem0.md` | 6KB 完整文档 |
| 5 | 贡献矩阵 | `ai_agent_study/SYSTEM.md` | ③记忆层(🔥🔥🔥) + ⑥进化层(🔥) |

### 新增/修改文件清单

**新增 3 个：**
- `mem0_hybrid_search.py` — HybridSearcher 类，sigmoid 归一化 BM25，entity boost 0.5
- `mem0_entity_extractor.py` — extract_entities_rule() + extract_entities_llm() + EntityStore
- `skills/SKILL-mem0.md` — 完整使用文档（安装/配置/接口/故障排查）

**修改 2 个：**
- `evolver.py` — 新增 `record_agent_fact()` / `agent_facts_stats()` / `FACTS_PATH`，4处 edit
- `ai_agent_study/SYSTEM.md` — ③记忆层表格 + 贡献矩阵 + 落地状态表

**依赖安装：**
- `pip install rank-bm25` ✅

### 测试结果

- `mem0_hybrid_search.py "小谷"` → 155 文档段，BM25 检索正常返回
- `mem0_entity_extractor.py` → 4类实体提取正常（PROPER/QUOTED/COMPOUND/NOUN）
- `evolver.py facts` CLI → Agent Facts: total=2, success=1, failure=1 ✅

### 关键设计决策

1. **不创建 `mem0_agent_facts.py`** — 逻辑合入 `evolver.py` 模块函数，减少文件碎片
2. **`EvolverEngine.record()` 自动调用** — 每次记录规则时自动提取 Agent 事实
3. **memory_pipeline.py 不做** — qclaw 存储已是 add-only（追加文件），无需额外修改
4. **BM25 用 rank-bm25 而非手动实现** — 与 mem0 原版一致

### 核心技术点

- **融合公式**（来自 mem0 scoring.py）：`combined = (semantic + normalized_bm25 + entity_boost) / max_possible`
- **BM25 归一化**：sigmoid 函数，query-length 自适应参数（short/medium/long 三档）
- **阈值门控**：语义分 < 0.5 直接排除，BM25/实体不能"救活"
- **实体 boost**：每个匹配实体 +0.5 权重

---

## 待办更新

| # | 事项 | 优先级 | 状态 |
|---|------|--------|------|
| 1 | **mem0 落地方案（4项）** | 高 | ✅ 已完成（2026-05-16 00:53） |
## 01:10-01:24 lianghua 项目源码审查

小谷要求审查 lianghua 量化项目的源码并提出建议。按规矩（顾庸x的项目，只给建议不动手），完成了全面代码审查。

### 文件读取

- `E:\lianghua\config.py`（~90行）：参数配置中心
- `E:\lianghua\indicators.py`（~280行）：6 regime状态机 + 动态ATR/仓位 + 加权打分
- `E:\lianghua\STRATEGY.md`：策略文档（含7个参考项目总结）
- `E:\lianghua\trend_trader.py`（~700行）：主程 v2.2（GUI+凯利+多周期RSI+MACD背离）
- `E:\lianghua\trend_trader_v2.py`（~500行）：旧版 v2.0（简化版）

### 发现的P0问题（6个）

1. **K线周期矛盾**：STRATEGY.md/config.py 写 4h，但 trend_trader.py 用 `interval="1h"` — 策略核心参数不一致
2. **config.py 无人引用**：trend_trader.py 完全重定义了所有参数，两处独立维护
3. **跟踪止盈 bug**：trailing_price 只在开仓初始化一次，从不更新，导致跟踪止盈实际无效
4. **固定止盈缺失**：check_stop_loss_and_profit() 只检查了止损+跟踪止盈，没检查 TAKE_PROFIT_ATR
5. **API Key 明文存储**：api_key.txt + api_secret.txt 明文在项目目录
6. **testnet 硬编码**：create_binance_client() 中 testnet=True 硬编码，与 PAPER_SIMULATE 无关

### P1问题（4个）

7. STRATEGY.md 与代码 5 处不一致（杠杆3x→1.5x，每日2笔→5笔，K线4h→1h，只做多→多空双向，连亏2→3笔）
8. 指标函数在 indicators.py 和 trend_trader.py 中重复实现且 RSI 处理方式不同
9. state.json 恢复无校验（过期熔断日期不会被清理）
10. 计数器逻辑分散在 close_position 调用方而非集中管理

### P2优化（5个）

- 回测缺失、异常处理过宽、GUI耦合、仓位太轻(0.005 BTC)、MACD算法不一致

### 审查结论

优先修复顺序：K线周期统一 → import config.py → 加固定止盈 → 修复跟踪止盈 → testnet 动态切换 → 更新 STRATEGY.md → 统一信号计算 → state.json 校验
## 01:30-01:44 lianghua 补充审查

读完了 IMPLEMENTATION_PLAN.md、mimic_trader.py、state.json、trades.csv 后，出具第二版建议。

### 新发现

1. **IMPLEMENTATION_PLAN.md 计划与代码脱节**：计划文档反复引用 `analyze_market()`/`open_position()`/`check_and_close()`，但 trend_trader.py 实际是 trading_loop() 内联逻辑，没拆出这些函数。计划写"已写入"但代码结构对不上。

2. **mimic_trader.py 不是跟单机器人**：只是 auto_start 功能的测试脚本，项目名称有误导。

3. **trades.csv 只有 SELL 记录**：3条全是 SELL(paper)，没有 BUY。开仓日志可能漏记。

4. **跟踪止盈 bug 已确认（实盘直接亏钱）**：trailing_price 只在开仓设一次从不更新，价格创新高后永远不会触发新的跟踪条件。计划文档讨论的是倍数自适应，没触及这个根本逻辑缺陷。

5. **state.json 极简**：只有4个字段（auto_start/connected/running/position），20+字段的关键状态（持仓价/止损价/跟踪价）崩溃后全部丢失。

### 最终8项修复顺序

① K线周期统一 ② from config import * ③ 固定止盈补检查 ④ 跟踪止盈逻辑修复 ⑤ 统一信号计算(用indicators.py) ⑥ testnet动态切换 ⑦ 代码结构对齐IMPLEMENTATION_PLAN ⑧ state.json完整保存
## 01:50-01:55 lianghua 第三轮审查 — 架构层面发现

读完 config.py、indicators.py、monitor.py 后，发现了比单点 bug 更严重的结构性问题：

### 核心问题：两套独立信号系统并存

indicators.py（280行，numpy ewm标准实现，6状态机，自适应ATR/仓位，分函数规范）完全没被 trend_trader.py import。趋势代码把所有指标、参数、逻辑全部重写了一遍。

**indicators.py 有但 trend_trader.py 没用上的：**
- 6 regime 状态机 vs 简单的 TREND/RANGE 二分
- get_position_size_pct() 动态八档仓位 vs 凯利公式
- should_forbid_new_position() 统一入口 vs 零散检查
- get_adaptive_atr_multipliers() vs 全局固定值
- is_bear_market() 独立函数 vs 内联逻辑

**关键证据：** monitor.py 成功 import indicators 并用 4h K线，而 trend_trader.py 用的是 1h。同一项目两个周期。

**indicators.py 实际已实现了 IMPLEMENTATION_PLAN 90% 的改进！只需 import 就行。**

### 本轮新发现的遗漏问题
- MIN_TRADE_INTERVAL=7200 在 config 但无人 enforce
- GLOBAL_STOP_LOSS=0.08 无任何检查
- API_MAX_RETRIES=3 等重试机制未落地
- 布林带阈值三处三种表达方式

### 最终修复路线
① trend_trader.py import config + import indicators，删所有重复
② 补 GLOBAL_STOP_LOSS / MIN_TRADE_INTERVAL / tracking / TP / testnet
## 02:00-02:20 lianghua 第四轮审查 — 文档与版本演进

读完 VERSION_LOG v2.0-v2.3 和 STRATEGY.md，发现新层面问题：

### 版本迭代时间线（全部今天）
v2.0(22:20) → v2.1(22:35) → v2.2(23:50) → v2.3(00:40)
四版在 2h20m 内完成，持续加功能，从未重构架构。

### 核心发现

**1. STRATEGY.md 几乎全错**
- 杠杆: 文档3x vs 代码1.5x
- 方向: 文档"只做多" vs 代码多空双向
- 每日笔数: 文档2笔 vs 代码3-5动态
- K线: 文档4h vs 代码1h
- 净值跌8%平仓: 文档铁律 vs 代码无检查
- 文件结构: 文档列5个 vs 实际80+

**2. 文件组织失控** — 80+文件，大量测试/临时脚本（click_*.py, watch*.ps1, *_err.log等）。推荐核心文件≤15个，其余移入_archive/。

**3. 跟踪止盈bug从v2.0就有** — 原始代码已确认trailing_price只设一次不更新，历经4版未修复。

**4. GLOBAL_STOP_LOSS=0.08** — 文档和config都有，但4个版本代码中均无实现。

**5. stock-daily-analysis-skill** — 独立的GitHub项目被整个嵌入lianghua根目录。

### 五层修复路线
① 文档对齐(STRATEGY.md) ② 文件清理 ③ 代码缝合(import config+indicators) ④ 补漏(tracking/TP/GLOBAL_STOP/testnet) ⑤ 安全(API Key环境变量)
## 02:25-02:45 lianghua 第五轮审查 — 重大纠正

### 关键纠正：之前审查基于 v2.0 备份，v2.3 实际已修复大量问题

读取 trend_trader.py v2.3 实际代码（55KB/1305行）后发现：

**已修复（我之前批错了）：**
- `from config import *` ✅ (L22)
- `from indicators import calculate_signal, compute_all_indicators` ✅ (L15)
- `from indicators import is_bear_market, detect_regime` ✅ (L1078)
- indicators.py 六态门控已接入：`should_forbid_new_position()` ✅ (L1087)
- `testnet=PAPER_SIMULATE` 动态切换 ✅ (L229)
- 动态仓位 `compute_position_from_balance()` ✅ (L245)
- MIN_TRADE_INTERVAL 已实现 ✅ (L1119-1128)
- **跟踪止盈已修复** ✅ (L948-949: `state["trailing_price"] = price`)
- 凯利/多周期RSI/自适应ATR 全部存在且被调用 ✅
- 科学熔断 _record_atr_stop 已实现 ✅

**仍存在的问题（仅4个）：**
1. 固定止盈TP：`take_profit_price` 存储了但 `check_stop_loss_and_profit()` 不检查
2. GLOBAL_STOP_LOSS：函数存在(L972)但空壳，peak_balance不更新不比较
3. K线周期：代码仍用1h，config和文档写4h
4. STRATEGY.md：严重过时（"只做多"/"3x"/"每天2笔"全错）

### 教训
审查前需确认读的是实际运行版本，不是备份。v2.3 在架构缝合上做得比预期好很多。
## 02:45-03:00 lianghua 第六轮审查 — IMPLEMENTATION_PLAN 落地验证

读完 IMPLEMENTATION_PLAN.md（完整版）和 state.json 后，做了 PLAN 声称 vs 实际代码的对照。

### 核心发现：PLAN 里 2/4 项"已写入"并不存在

| 改进 | PLAN说法 | 实际v2.3代码 |
|------|---------|------------|
| ① 六态市场识别 | ✅ 已写入 | ✅ should_forbid_new_position() (indicators.py) |
| ② 动态ATR倍数 | ✅ 已写入 | ❌ get_adaptive_atr_multipliers 不存在 |
| ③ 出场实时判断 | ✅ 已写入 | ✅ prev_trend 已移除 |
| ④ 双指标确认 | ✅ 已写入 | ❌ ema_confirm & macd_confirm 不存在 |

②和④在PLAN文档中列举了完整代码示例但未写入v2.3。

**PLAN 函数命名与代码对不上**：
- PLAN: analyze_market() / open_position() / check_and_close()
- 实际: trading_loop() 内联全部逻辑，未拆分函数
- PLAN文档对后来者具有误导性

### 新发现

**1. state.json 持久化不完整**
- 开仓时 `state["take_profit_price"]` 存入(L1107)但 state.json 无此字段
- ema20/50/200, account balance, peak_balance 均未持久化
- 程序崩溃后持仓恢复会丢失止盈目标价
- 当前 state.json 有12个字段但缺关键交易状态

**2. 日志噪音严重**
- 每轮输出4条日志（7分制+100分制+多RSI+状态枚举）
- 15秒循环 = 每天23,040行日志
- 建议加 verbose 开关

**3. ATR使用不一致**
- 开仓时用标准ATR（compute_signal返回值）
- 持仓检查时用自适应ATR（compute_adaptive_atr）
- 同一仓位开仓止损和后续止损ATR计算方式不同

### 六轮审查覆盖矩阵

| 轮次 | 视角 | 关键发现 |
|------|------|---------|
| 1 | 代码bug | 跟踪止盈、TP缺失、testnet硬编码等6个P0 |
| 2 | 架构 | config/indicators未对接，IMPLEMENTATION_PLAN脱节 |
| 3 | 深层架构 | 两套信号系统并存，indicators.py已实现90%改进 |
| 4 | 文档/版本 | STRATEGY.md严重过时(6处错误)，80+文件混乱 |
| 5 | 自我纠正 | v2.3已修80%问题，之前看的是v2.0备份 |
| 6 | 落地验证 | PLAN2/4项虚报，state持久化不完整，日志噪音 |

### 当前实际待修（准确版）

v2.3 真正剩下：
1. 固定止盈TP检查（take_profit_price存了但不用）
2. GLOBAL_STOP_LOSS空壳（peak_balance从不更新比较）
3. K线周期统一为4h
4. STRATEGY.md同步更新
5. state.json补充take_profit_price等关键字段
6. IMPLEMENTATION_PLAN ②④两项落地或从PLAN中移除
## 10:37-10:55 lianghua 第七轮审查 — 代码质量 + 安全审计

### 🔴 严重问题

**1. state.json 非原子写入 — 崩溃数据全丢**
- `save_state()` (L197): 直接 `open(STATE_FILE, "w")` + `json.dump`
- 没有 `.tmp` + `os.replace()` 原子模式
- 没有备份上次成功保存的状态
- 写到一半崩了 = state.json 损坏 = 重启后恢复不了持仓

**2. 线程安全：state 字典零保护**
- 88 次访问 state 字典
- `_client_lock` 只保护 Client 对象（L223，只用了1次）
- trading_loop 线程不断写 state，GUI 线程同时读
- 没有任何锁保护 state，读脏数据概率低但后果严重

**3. API Key 在 GUI 明文显示**
- `api_key_entry` (L1206): **没有 `show="*"`** — 输入的 Key 明文可见
- `api_secret_entry` (L1213): 有 `show="*"` — Secret 遮住了
- Key 和 Secret 同样敏感，应都遮住

### 🟠 高优先级

**4. 裸 `except Exception` — 22 处**
- 8处连日志都不打（L192,206,287,300,325,912,1015,1127）
- `save_state()` 的 `except Exception: pass` — 状态保存失败完全不知道

**5. 30 秒 debounce — 可以丢关键状态**
- L200: `if now - _last_save_time < 30: return`
- 一个订单的完整生命周期可能 < 30 秒
- 如果在这 30 秒内崩溃，订单状态全丢

**6. requirements.txt 依赖缺失**
- 只有：`python-binance>=1.0.19`
- 代码实际需要：`pystray` ❌ `Pillow (PIL)` ❌ `numpy` ❌ `requests` ❌
- 其他人拿到 requirements.txt 装不全依赖

### 🟡 中优先级

**7. 内联 import — 散乱且可能重复**
- `from indicators import ...` 在 L15 和 L1083 两次 import
- `import requests` 在 L279/292/317 三次 import
- 所有 import 应该在文件顶部

**8. 没有信号处理器**
- Ctrl+C / 进程 kill → `save_state()` 不调用
- `on_exit()` 依赖 GUI 的 `root.destroy()`

**9. get_klines() 无超时控制**
- 如果 Binance API 响应慢，get_klines() 会阻塞整个循环
- 持仓期间阻塞 = 止损止盈检查暂停

### 七轮审查总览

| 轮次 | 视角 | 发现重点 |
|------|------|---------|
| 1 | 代码bug | 跟踪止盈、TP缺失、testnet硬编码 |
| 2 | 架构 | config/indicators未对接 |
| 3 | 深层架构 | 两套信号系统并存 |
| 4 | 文档/版本 | STRATEGY.md过时，80+文件 |
| 5 | 自我纠正 | v2.3已修80%，之前看的是v2.0备份 |
| 6 | 落地验证 | PLAN里2/4项虚报 |
| 7 | **质量/安全** | **线程安全+非原子写+Key明文+22处裸except** |

### 最终修复优先级（准确版）

1. state.json 原子写入 + 备份
2. state 字典加锁（或用 `threading.Lock()`）
3. API Key 输入框加 `show="*"`
4. 裸 `except` 补日志
5. debounce 从30秒降到5秒或去掉
6. requirements.txt 补全依赖
7. import 统一到文件顶部
8. 加信号处理器（SIGINT/SIGTERM）
9. get_klines() 加超时

---

## 13:54 会话状态核对

**小谷问"做完了吗"，顾庸如实汇报：**

| 任务 | 状态 | 备注 |
|------|------|------|
| mem0 四模块落地 | ✅ 完成 | 00:30-00:53，hybrid_search + entity_extractor + agent_facts + SKILL |
| lianghua 七轮审查 | ✅ 完成 | 01:10-10:55，代码bug→架构→文档→安全全覆盖 |
| TencentDB-Agent-Memory | ❌ 未做 | 仓库已 git fetch 恢复（E:\ai\TencentDB-Agent-Memory\），架构提取待进行 |

**TencentDB-Agent-Memory 当前状态：**
- 仓库路径：`E:\ai\TencentDB-Agent-Memory\`
- 17个文件已恢复（git fetch + git reset --hard origin/main）
- 源码未读、架构未提、落地未开始
- 等待小谷确认是否继续
---

## 14:00-14:35 TencentDB-Agent-Memory 完整落地 ✅

小谷问"做完了吗"后确认"该做就做"，顾庸完成了 TencentDB-Agent-Memory 的 Python 移植。

### 学习过程

1. 读取架构文档：design.md, architecture.md, context-offloading.md
2. 读取 TypeScript 核心源码：checkpoint.ts, l1-extractor.ts, memory-pipeline-manager.ts, session-gc.ts, managed-timer.ts
3. 提取核心设计：split-state checkpoint, atomic capture, downward-only L2 timer, warm-up mode, session GC

### 落地成果

| # | 落地项 | 文件 | 说明 |
|---|--------|------|------|
| 1 | MemoryPipelineManager | `tdai_memory_pipeline.py` | ~47KB Python 完整移植 |
| 2 | CheckpointManager | 同上 | 原子 JSON (tmp+rename) + backward compat |
| 3 | SerialQueue | 同上 | 串行任务队列 (L1/L2/L3各一) |
| 4 | ManagedTimer | 同上 | 可重置/向下提前定时器 |
| 5 | Skill 文档 | `skills/SKILL-tdai-memory.md` | 5.8KB 完整文档 |
| 6 | 贡献矩阵 | `ai_agent_study/SYSTEM.md` | ③记忆层(🔥🔥🔥🔥🔥) + ②认知层(🔥🔥) + ⑥进化层(🔥🔥) |

### 新增/修改文件清单

**新增 2 个：**
- `tdai_memory_pipeline.py` — MemoryPipelineManager + CheckpointManager + SerialQueue + ManagedTimer + SessionFilter + SessionGC + create_l1_runner + create_l3_runner + CLI demo
- `skills/SKILL-tdai-memory.md` — 快速开始/接口/配置/checkpoint格式/对比矩阵

**修改 1 个：**
- `ai_agent_study/SYSTEM.md` — ③记忆层表 + ⑥进化层表 + 贡献矩阵 + 落地状态

**所有文件均通过 qclaw-text-file write_file.py 写入（UTF-8, Windows 平台）**

### 核心设计（从 TypeScript 提取）

1. **Split-state Checkpoint**: runner_states(CheckpointManager管) 和 pipeline_states(PipelineManager管) 分离，防止互相覆盖
2. **Atomic capture**: captureAtomically() 在读-改-写锁内执行
3. **Downward-only L2 timer**: tryAdvanceTo() — 只能提前不能推迟
4. **Warm-up mode**: 新会话阈值 1→2→4→...→N
5. **Session GC**: 按 last_active_time 淘汰冷会话

### 测试验证

```bash
E:\PYTON\python.exe tdai_memory_pipeline.py
```
- 10轮对话模拟 → 4批 L1 提取触发
- Warm-up: 1≥1(触发) → 2≥2(触发) → graduated(稳态3) ✅
- L2 timer 正常触发 ✅
- Graceful destroy flush 正确 ✅
- 所有队列清空 ✅

### 与现有系统集成

- `create_l1_runner(workspace_dir)` → 调用 evolver.record_agent_fact()
- `create_l3_runner(workspace_dir)` → 生成 persona_tdai.md stub
- 与 mem0_hybrid_search.py + mem0_entity_extractor.py 形成完整记忆栈

### 待完善

- L1 runner: 接入 OpenClaw LLM 做结构化提取（当前 stub）
- L2 runner: 场景聚合（当前 stub）
- L3 runner: LLM 生成 persona（当前 stub）
- Context Offloading: Mermaid 符号记忆（仅 TypeScript 源码存在）

### 关键认知

- TDAI 的四层记忆 (L0→L1→L2→L3) 是 qclaw 现有记忆系统（MEMORY.md + memory/日期.md + evolver）的升级路径
- Warm-up mode 解决新会话信息空白问题（快速建立初始记忆画像）
- Downward-only L2 timer 是一种精巧的调度设计：场景提取可提前但不可推迟，避免永远不触发
- Split-state checkpoint 是并发安全的关键保证（TypeScript 刻意设计，Python 版继承）