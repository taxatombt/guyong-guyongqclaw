# 2026-04-30

### 11:56 - Anthropic Managed Agents 完整落地

小谷第6次要求，终于完成。4个核心模块 + SKILL.md + 验证脚本，全部写入并通过测试。

**新增文件（skills/managed-agents-study/）：**
- `session_vault.py` (9KB) — Session外置持久化 + getEvents() + wake()
- `skill_metadata.py` (8.5KB) — 技能元数据 + 渐进式披露（Tier 0/1/2）
- `subagent_protocol.py` (8.1KB) — Subagent通信协议 + 结果压缩
- `exec_isolation.py` (8.1KB) — Sandbox凭证隔离（Vault→inject→strip）
- `SKILL.md` (1.7KB) — 架构文档
- `run_all.bat` (1.9KB) — 验证脚本

**验证结果：4/4 PASS**
- session_vault: 2 events created
- skill_metadata: 126 skills discovered
- subagent_protocol: dispatch OK
- exec_isolation: exit=0, output=hello

**CMA三层架构完整映射：**
- Session → session_vault.py (append-only JSONL)
- Harness → agents/tool_pipeline.py (15步管道) + session_vault.wake()
- Sandbox → exec_isolation.py (Vault凭证隔离)

**经验教训：**
- PowerShell不支持`&&`语法，用`;`代替
- Python heredoc (`<< 'PYEOF'`) 在 PowerShell 不可用，用`write`工具代替
- 前几次失败都是因为文件写入方法不兼容Windows

# 2026-04-30 工作日志

### 16:56  -  Agent清理

- 用户要求删除qclaw系统中所有agent的会话数据（不删配置），包括sub-agent

### 01:27  -  ManagedAgents落地

- 完成agents/managed_bridge.py（16.7KB），7打通点全部20/20测试通过，一行初始化调用Managed Agents能力
- 三层架构：Session外置记忆→Harness决策循环→Sandbox执行双手，虚拟化让接口比实现活得久
- MCP=标准化连接解决够得着外部资源，Skills=领域知识封装解决知道怎么做，互补非竞争
- 技能渐进式披露：元数据(~100token)→SKILL.md主体(~1-5k)→附加资源按需访问
- 模块代码已落地在workspace但未接入qclaw运行时主循环，需改openclaw配置或主逻辑才自动生效
- 用户要求不改底层代码，现状为最终态：研究完成+模块桥接代码都在workspace，qclaw运行时不动
