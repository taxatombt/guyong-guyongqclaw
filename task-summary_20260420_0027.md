# 任务摘要：blender-mcp 深度落地

**时间**：2026-04-20 00:27

## 做了什么

1. **完整源码拉取**（addon.py 2635行 + server.py 1186行 + telemetry.py 300行）
2. **源码级分析**
3. **SKILL.md 更新**：blender_mcp_study/SKILL.md（14KB）
4. **MEMORY.md 更新** + **Git commit**: 4380e41

## 核心发现

### telemetry.py（最高价值）
- **consent-gated两级数据**：无同意只收tool_name/success/duration，不收prompt/metadata
- **匿名UUID持久化**：APPDATA/BlenderMCP/，Unix 0o600权限
- **环境变量禁用**：DISABLE_TELEMETRY / BLENDER_MCP_DISABLE_TELEMETRY / MCP_DISABLE_TELEMETRY
- **后台Queue+Worker**：队列满自动丢弃（graceful degradation），不阻塞主线程
- **@telemetry_tool装饰器**：自动追踪每个工具的执行时间+成功率

### addon.py（Blender插件）
- **bpy.app.timers.register**：在Blender主线程异步执行命令（GUI不死）
- **context.temp_override**：截图时切换3D视口，不打断用户当前操作
- **ZIP Slip双重检查**：abs_path前缀检查 + ".."序列检查
- **动态handler注册**：按addon偏好启用/禁用 PolyHaven/Hyper3D/Sketchfab/Hunyuan3D

### server.py（核心通信）
- **分块JSON接收**：每块尝试解析，完整才退出，超时用已有数据
- **180s超时同步**：与addon保持一致的socket超时
- **持久连接+健康检测**：全局单例，发ping验证

## 最高价值落地点

1. **telemetry.py → qclaw evolver**：加consent层+匿名UUID+后台队列
2. **server.py receive_full_response → socket处理**：分块+完整性验证
3. **addon.py 动态handler注册 → skill路由**：按配置启用/禁用
