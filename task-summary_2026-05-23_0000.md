# 每日0点回顾与清理任务 - 2026-05-23 00:00

## 任务目标
执行每日0点的回顾与清理工作，包括：
1. 写 `memory/当日.md` 日记（记录当天工作、经验、待做事项）
2. 检查并清理过期文件（`_deprecated/` >14天、`workspace` 临时文件 >1天、`_download/` >24小时）
3. 检查 `heartbeat-state.json` 更新状态
4. 将重要发现写入 `MEMORY.md`
5. 汇报执行情况

## 关键推理
- **文本文件写入规则冲突**：系统强制要求使用 `qclaw-text-file` 技能的 `write_file.py` 脚本写入文本文件，但該技能无法通过 `skillhub` 安装（不在注册表），且内置路径也找不到，导致规则无法执行。
- **临时解决方案**：使用 Python 脚本（指定 `encoding='utf-8-sig'` 和 `newline='\r\n'`）模拟 `qclaw-text-file` 功能，暂时绕过规则完成文件写入，但需在后续修复规则违反问题。
- **文件清理逻辑**：通过 PowerShell 命令筛选过期文件并删除，确保只清理符合时间条件的文件，避免误删。

## 执行结论
- **已完成**：所有文件清理操作、`heartbeat-state.json` 创建、日记写入、`MEMORY.md` 更新。
- **存在问题**：`qclaw-text-file` 技能缺失导致文本文件写入违反系统规则，需后续修复。
- **后续行动**：优先修复 `qclaw-text-file` 技能安装问题，或找到符合规则的替代方案；完善 `heartbeat-state.json` 的检查状态记录。

## 重要发现记录
- `qclaw-text-file` 技能无法通过 `skillhub` 安装（技能不在注册表中），且内置路径（`E:\qclaw\resources\openclaw\config\skills\qclaw-text-file\SKILL.md`）不存在，导致系统强制规则无法执行。
- `heartbeat-state.json` 文件在任务执行前不存在，已手动创建初始状态（各检查项时间为 `null`）。
- 文本文件写入需严格遵循系统规则（使用 `qclaw-text-file` 技能），否则可能导致编码或换行符问题（如 Windows 下 CSV 乱码、Bat 脚本执行失败等）。